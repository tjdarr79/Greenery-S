GREENERY-S UPDATE 4 — install docs, controls card, tools/ split

Apply AFTER update 3. Copy into C:\Users\Travis\Documents\GitHub\Greenery-S,
replace when asked, then in GitHub Desktop DELETE the old top-level
dump-relay.py (it has moved to tools\).

Delete this file before committing.

COMMIT MESSAGE
--------------
Fresh-install path, controls dashboard card, tools/ split

Adds dashboard-controls.yaml with the Task Mode buttons and their confirmation
guards, a six-step ordered install section in the README, and a file inventory
so it is clear which files are runtime and which are optional.

Moves the diagnostic scripts into tools/ with a README explaining that neither
is needed to run the bridge. The apply_*.py patch scripts are deliberately
excluded: farm_bridge.py already contains everything they added.

FILES
-----
README.md                          New install section + repo file map.
dashboard-controls.yaml            NEW. Task Mode card.
tools\dump-relay.py                MOVED from repo root - delete the old one.
tools\discover-farmhand-api.py     NEW.
tools\README.md                    NEW.
